const assert = require('assert')
const a = 5
const b = "5"
assert.deepStrictEqual(a,b,"a and b value and datatype is not equal")
// its check two value are equal or not 
// The comparison is performed with === operator 