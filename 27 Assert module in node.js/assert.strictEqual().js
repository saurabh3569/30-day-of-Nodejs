const assert = require('assert')
const a = 10
const b = 20
assert.strictEqual(a, b, "Error acc to strict equality comparison")
// its checking both value are strict equal or not its using ( === ) comparison 